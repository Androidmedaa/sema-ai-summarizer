# Backend Environment Variables
# Bu dosyayı kopyalayıp .env olarak kaydedin ve değerleri doldurun

# Server Port
PORT=5000

# JWT Secret Key (Güvenlik için çok önemli!)
# Güçlü bir secret key oluşturmak için: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Google Gemini API Key (İsteğe bağlı - AI özellikleri için)
# Gemini API anahtarı almak için: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# CORS Origins (Vercel deployment için önemli!)
# Development için: http://localhost:3000,http://127.0.0.1:3000
# Production için: Vercel deployment sonrası aldığınız frontend domain'ini ekleyin
# Örnek: https://your-frontend-app.vercel.app,https://your-custom-domain.com
# Birden fazla origin için virgülle ayırın
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
